// Este componente foi substituído pela página /admin/suap/page.tsx
// Mantido apenas para compatibilidade com imports antigos
export function SyncButtons({ syncSecret, suapConfigurado }: {
  syncSecret: string;
  suapConfigurado: boolean;
}) {
  return null;
}
